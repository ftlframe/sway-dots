exec swayidle -w \
    timeout 300 'swaylock -f -c 080709' \
    timeout 600 'swaymsg "output * power off"' \
    resume 'sleep 1 && swaymsg "output * power on"' \
    before-sleep 'swaylock -f -c 080709'
